import React from 'react';
import AboutComponent from '../components/About';

const About: React.FC = () => {
    return (
        <div className="min-h-screen pt-20 flex items-center justify-center pb-10">
            <AboutComponent />
        </div>
    );
};

export default About;
